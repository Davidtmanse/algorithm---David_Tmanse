```
// e[]表示节点的值，l[]表示节点的左指针，r[]表示节点的右指针,idx表示当街用到了哪个节点
int e[N], l[N], r[N], idx;

// 初始化
void init(){
    // 0是左端点，1是右端点
    l[1] = 0, r[0] = 1;
    idx = 2;
}

// 在节点a的右边插入一个数x
void insert(int a, int x){
    e[idx] = x;
    l[idx] = a, r[idx] = r[a];
    l[r[a]] = idx, r[a] = idx ++;
}

// 删除结点a
void remove(int a){
    l[r[a]] = l[a];
    r[l[a]] = r[a];
}
```

### 827.[双链表]()


未成功的写法
```
#include <iostream>
#include <string>

using namespace std;

const int N = 100010;
int e[N], l[N], r[N], idx;

void init(){
    l[1] = 0;
    r[0] = 1;
    idx = 2;
}

void insertHeadL(int x){
    e[idx] = x;
    r[idx] = 0, l[idx] = l[1];
    r[l[1]] = idx, l[1] = idx ++;
}

void insertHeadR(int x){
    e[idx] = x;
    l[idx] = 1, r[idx] = r[0];
    l[r[0]] = idx, r[0] = idx ++;
}

void remove(int k){
    l[r[k]] = l[k];
    r[l[k]] = r[k];
}

void insertL(int k, int x){
    e[idx] = x;
    r[idx] = k, l[idx] = l[k];
    r[l[k]] = idx, l[k] = idx ++;
}

void insertR(int k, int x){
    e[idx] = x;
    l[idx] = k, r[idx] = r[k];
    l[r[k]] = idx, r[k] = idx ++;
}


int main(){
    int m;
    cin >> m;
    
    init();
    
    while (m --){
        string op;
        cin >> op;
        
        int k, x;
        
        if (op == "L"){
            cin >> x;
            insertHeadL(x);
        }
        else if (op == "R"){
            cin >> x;
            insertHeadR(x);
        }
        else if (op == "D"){
            cin >> k;
            remove(k);
        }
        else if (op == "IL"){
            cin >> k >> x;
            insertL(k + 1, x);
        }
        else if (op == "IR"){
            cin >> k >> x;
            insertR(k + 1, x);
        }
    }
    
    
    for (int i = r[0]; i != -1; i = r[i]) 
        cout << e[i] << ' ';
    cout << endl;
    return 0;
}
```

成功的写法(y的)
```
#include <iostream>

using namespace std;

const int N = 100010;

int m;
int e[N], l[N], r[N], idx;

// 在节点a的右边插入一个数x
void insert(int a, int x)
{
    e[idx] = x;
    l[idx] = a, r[idx] = r[a];
    l[r[a]] = idx, r[a] = idx ++ ;
}

// 删除节点a
void remove(int a)
{
    l[r[a]] = l[a];
    r[l[a]] = r[a];
}

int main()
{
    cin >> m;

    // 0是左端点，1是右端点
    r[0] = 1, l[1] = 0;
    idx = 2;

    while (m -- )
    {
        string op;
        cin >> op;
        int k, x;
        if (op == "L")
        {
            cin >> x;
            insert(0, x);
        }
        else if (op == "R")
        {
            cin >> x;
            insert(l[1], x);
        }
        else if (op == "D")
        {
            cin >> k;
            remove(k + 1);
        }
        else if (op == "IL")
        {
            cin >> k >> x;
            insert(l[k + 1], x);
        }
        else
        {
            cin >> k >> x;
            insert(k + 1, x);
        }
    }

    for (int i = r[0]; i != 1; i = r[i]) cout << e[i] << ' ';
    cout << endl;

    return 0;
}
```